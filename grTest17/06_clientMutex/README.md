Что нужно реализовать:
    Создать контекст с тайм-аутом (например, 5 секунд).
    Запустить N горутин (обработка клиентов).
    Каждая горутина должна:
    Проверять контекст на отмену.
    Обрабатывать "клиента" — увеличить счетчик обработанных (с мьютексом).
    Основной поток должен дождаться завершения всех горутин (например, через sync.WaitGroup).
    По окончании вывести количество обработанных клиентов.